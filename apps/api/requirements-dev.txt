# Direct development dependencies, human-readable.
# Install from requirements-dev.lock.txt instead. See requirements.txt.
-r requirements.txt

pytest>=8.3
pytest-django>=4.9
# HTTP stubbing: no test may reach Google's live servers.
responses>=0.25
