# Direct runtime dependencies, human-readable.
#
# Install from requirements.lock.txt instead — it pins this set and every
# transitive dependency to an exact version. Edit this file to add or move a
# dependency, then regenerate the locks with ./scripts/lock-python-deps.sh and
# commit both changes together.
Django>=5.1,<5.3
djangorestframework>=3.15
psycopg[binary]>=3.2
argon2-cffi>=23.1
dj-database-url>=2.2
python-dotenv>=1.0
# Google's maintained OAuth library. Pulls in google-auth; used so the
# protocol is not hand-built.
google-auth-oauthlib>=1.2
# HTTP client for the Google Admin APIs. Already present transitively via
# google-auth-oauthlib; declared here because integrations/google/ga4.py calls
# it directly, and a direct import must not rest on another package's
# dependency choices.
requests>=2.32
# WSGI server for containers. The Django development server is never used
# outside local development.
gunicorn>=23.0
